
namespace Exercise1.Validators;

public class RegistrationValidator : AbstractValidator<Registration>
{
    public RegistrationValidator()
    {
        // RuleFor(x => x.Plate).NotEmpty().WithMessage("Plate is required");
        // RuleFor(x => x.Start).NotEmpty().WithMessage("Start date is required");
        // RuleFor(x => x.End).NotEmpty().WithMessage("End date is required");
        // RuleFor(x => x.CarId).NotEmpty().WithMessage("CarId is required");
        // RuleFor(x => x.TotalPrice).NotEmpty().WithMessage("TotalPrice is required");
        // RuleFor(x => x.IsFinished).NotEmpty().WithMessage("IsFinished is required");
    }
}